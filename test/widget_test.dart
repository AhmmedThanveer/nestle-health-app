import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder smoke test', (WidgetTester tester) async {
    // Replace with real integration tests as features ship.
    expect(true, isTrue);
  });
}
